// src/constants/colors.ts
// -----------------------------------------------------------------------------
// Central place for reusable colour tokens.
// -----------------------------------------------------------------------------

/** Background tint for Iranian holidays in FullCalendar */
export const HOLIDAY_BG = '#ffeb3b55' // translucent yellow

/** Background for activities that have been «seen» */
export const SEEN_EVENT_BG = '#FFF8A6' // pale yellow

/** Fallback colour when the CRM record has no custom colour */
export const DEFAULT_EVENT_BG = '#6c757d' // Bootstrap “secondary”

export const DEFAULT_EVENT_BORDER = '#000000'

/** Priority-dot colours (CSS still hard-codes them) */
export const PRIO_HIGH = '#dc3545'
export const PRIO_MID = '#ffc107'
export const PRIO_LOW = '#28a745'
