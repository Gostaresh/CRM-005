{
  "name": "crm-005-monorepo",
  "version": "1.0.0",
  "private": true,                
  "packageManager": "npm@10.6.0",         
  "workspaces": [
    "apps/*",
    "packages/*"
  ],

  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,blue \"npm run dev --workspace=@gostaresh/crm-backend\" \"npm run dev --workspace=@gostaresh/crm-frontend\"",
    "dev:backend": "npm run dev --workspace=@gostaresh/crm-backend",
    "dev:frontend": "npm run dev --workspace=@gostaresh/crm-frontend",
    "build": "npm run build --workspace=@gostaresh/crm-backend && npm run build --workspace=@gostaresh/crm-frontend",
    "lint": "eslint . --ext .js,.ts,.vue",
    "test": "npm run test --workspace=@gostaresh/crm-backend"
  },

  "devDependencies": {
    "concurrently": "^8.0.1",
    "eslint": "^9.0.0",
    "eslint-plugin-vue": "^9.18.0"
  }
}